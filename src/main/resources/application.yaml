spring:
  application:
    name: whose-turn
  datasource:
    url: jdbc:postgresql://localhost:5432/whose-turn
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
