spring:
  application:
    name: whose-turn
  datasource:
    username: test
    password: test
    url: jdbc:postgresql://localhost:5432/whose-turn
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: update
